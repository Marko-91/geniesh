{
  "name": "geniesh",
  "version": "2.0.1",
  "type": "module",
  "bin": {
    "geniesh": "./src/cli.js",
    "gensh": "./src/cli.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "ora": "^9.4.0"
  },
  "scripts": {
    "build": "esbuild src/cli.js --bundle --platform=node --target=node18 --format=esm --outfile=dist/cli.js --external:ora --external:commander",
    "test": "jest"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.29.5",
    "@types/jest": "^30.0.0",
    "babel-jest": "^30.3.0",
    "esbuild": "^0.28.0",
    "jest": "^30.3.0"
  },
  "jest": {
    "testEnvironment": "node",
    "transform": {
      "^.+\\.[mc]?js$": "babel-jest"
    }
  }
}
